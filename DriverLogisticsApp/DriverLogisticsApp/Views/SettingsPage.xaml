<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DriverLogisticsApp.Views.SettingsPage"
             xmlns:viewmodels="clr-namespace:DriverLogisticsApp.ViewModels"
             x:DataType="viewmodels:SettingsPageViewModel"
             Title="Settings">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="10">
            <BoxView Margin="0,20,0,0" HeightRequest="1" Color="LightGray" />

            <BoxView Margin="0,20,0,0" HeightRequest="1" Color="LightGray" />

            <Label
                FontAttributes="Bold"
                FontSize="Large"
                Text="Legal" />
            <Grid Margin="0,10,0,0" ColumnDefinitions="*,Auto">
                <Label Text="Privacy Policy" VerticalOptions="Center">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding GoToPrivacyPolicyCommand}" />
                    </Label.GestureRecognizers>
                </Label>
            </Grid>

            <BoxView Margin="0,20,0,0" HeightRequest="1" Color="LightGray" />
            <Label
                FontAttributes="Bold"
                FontSize="Large"
                Text="Appearance" />
            <Grid Margin="0,10,0,0" ColumnDefinitions="*,Auto">
                <Label Text="Enable Dark Mode" VerticalOptions="Center" />
                <Switch Grid.Column="1" IsToggled="{Binding IsDarkMode}" />
            </Grid>

            <BoxView Margin="0,20,0,0" HeightRequest="1" Color="LightGray" />

            <Label
                FontAttributes="Bold"
                FontSize="Large"
                Text="Security" />
            <Grid Margin="0,10,0,0" ColumnDefinitions="*,Auto">
                <Label Text="Enable PIN Authentication" VerticalOptions="Center" />
                <Switch Grid.Column="1" IsToggled="{Binding IsAuthenticationEnabled}" />
            </Grid>

            <VerticalStackLayout IsVisible="{Binding IsAuthenticationEnabled}" Spacing="10">

                <Entry
                    IsPassword="True"
                    Keyboard="Numeric"
                    MaxLength="4"
                    Placeholder="Enter New 4-Digit PIN"
                    Text="{Binding NewPin}" />

                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="5">
                    <Entry
                        Grid.Column="0"
                        IsPassword="True"
                        Keyboard="Numeric"
                        MaxLength="4"
                        Placeholder="Confirm New PIN"
                        Text="{Binding ConfirmPin}" />

                    <Image
                        Grid.Column="1"
                        IsVisible="{Binding PinsDoNotMatchErrorVisible}"
                        Style="{StaticResource ErrorIconStyle}" />
                </Grid>

                <Label
                    FontSize="Small"
                    IsVisible="{Binding PinsDoNotMatchErrorVisible}"
                    Text="PINs do not match."
                    TextColor="Red" />

                <Button Command="{Binding SetPinCommand}" Text="Set/Change PIN" />
            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
