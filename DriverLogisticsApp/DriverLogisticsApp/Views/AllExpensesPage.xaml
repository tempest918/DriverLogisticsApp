<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:DriverLogisticsApp.ViewModels"
             xmlns:models="clr-namespace:DriverLogisticsApp.Models.ExpenseTypes"
             x:Class="DriverLogisticsApp.Views.AllExpensesPage"
             Title="General Expenses"
             x:DataType="viewmodels:AllExpensesViewModel">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add General Expense" Command="{Binding GoToAddExpenseCommand}" />
    </ContentPage.ToolbarItems>
    <Grid>
        <CollectionView ItemsSource="{Binding Expenses}"
                        IsPullToRefreshEnabled="True"
                        RefreshCommand="{Binding LoadExpensesCommand}"
                        IsRefreshing="{Binding IsBusy, Mode=OneWay}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Expense">
                    <Border
                        Margin="10"
                        Padding="15"
                        Stroke="LightGray"
                        StrokeShape="RoundRectangle 10">

                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:AllExpensesViewModel}}, Path=GoToExpenseDetailsCommand}" CommandParameter="{Binding .}" />
                        </Border.GestureRecognizers>

                        <VerticalStackLayout>
                            <Label Text="{Binding FormattedDetails}" FontAttributes="Bold" />
                            <Label Text="{Binding Amount, StringFormat='{0:C}'}" />
                        </VerticalStackLayout>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            <CollectionView.EmptyView>
                <Label
                    HorizontalOptions="Center"
                    Text="No general expenses found."
                    VerticalOptions="Center" />
            </CollectionView.EmptyView>
        </CollectionView>
    </Grid>
</ContentPage>
